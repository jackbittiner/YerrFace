(function(exports) {

  function Picture(blob) {
    this.blob = blob;
    this.results = null;
    this.faces = [];
  }

  Picture.prototype.generateFace = function(json) {
    for(var key in json) {
      if (key === ) {

      }
    }
  }

  exports.Picture = Picture;

})(this);
